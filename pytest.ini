[pytest]
testpaths = tests
addopts =
    --cov=app
    --cov-report=term-missing:skip-covered
    --cov-report=xml
markers =
    integration: backend request-to-database integration tests
    unit: pure or narrow unit tests
    contract: API contract and serialization tests
