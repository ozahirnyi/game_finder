import { useQuery } from "@tanstack/react-query";
import { createFileRoute, Link } from "@tanstack/react-router";
import { AppShell } from "@/components/AppShell";
import { Avatar, GameCover } from "@/components/GameCover";
import { Chip, PresenceDot, SectionHeader } from "@/components/ui-bits";
import { getSteamSocial } from "@/lib/api";
import { lovableQueryKeys } from "@/lib/lovable-data";
import { Search, UserPlus, Gamepad2, MessageCircle } from "lucide-react";

export const Route = createFileRoute("/friends")({ head: () => ({ meta: [{ title: "Friends вЂ” GameFinder" }, { name: "description", content: "Your Steam friends and shared games." }] }), component: FriendsPage });

function FriendsPage() {
  const socialQuery = useQuery({ queryKey: lovableQueryKeys.steamSocial(12), queryFn: () => getSteamSocial(12) });
  const state = (socialQuery.error as { status?: number } | null)?.status;
  if (state === 401 || state === 409 || socialQuery.data?.steam.linked === false) return <AppShell><SectionHeader title="Friends" hint={state === 401 ? "Sign in to view Steam friends." : "Connect Steam to view Steam friends."} /><div className="rounded-3xl border border-border bg-surface p-6"><p className="text-sm text-muted-foreground">{state === 401 ? "Your session is signed out." : "Your Steam account is not linked."}</p><Link to="/steam" className="mt-4 inline-block rounded-lg bg-primary px-4 py-2 text-sm font-bold text-primary-foreground">Connect Steam</Link></div></AppShell>;
  const friends = socialQuery.data?.friends ?? [];
  const focus = friends[0];
  const sharedGames = friends.flatMap((friend) => friend.common_games.map((game) => ({ friend, game })));
  return <AppShell><div className="grid grid-cols-1 gap-10 lg:grid-cols-12"><div className="space-y-10 lg:col-span-8"><div><SectionHeader title="Friends" hint={`${friends.length} Steam friends`} action={<button disabled title="Adding friends is managed in Steam." className="flex items-center gap-2 rounded-lg bg-primary px-3 py-2 text-xs font-bold text-primary-foreground"><UserPlus className="size-3.5" /> Add friend</button>} /><div className="mb-6 flex items-center gap-3 rounded-xl border border-border bg-surface px-4 py-3"><Search className="size-4 text-muted-foreground" /><input disabled placeholder="Steam friend search is not available yet." className="flex-1 bg-transparent text-sm outline-none placeholder:text-muted-foreground" /></div><div className="space-y-3">{friends.map((f) => <div key={f.steam_id} className="grid grid-cols-[auto_minmax(0,1fr)_auto] items-center gap-4 rounded-2xl border border-border bg-surface p-4 transition hover:border-white/20 sm:grid-cols-[auto_minmax(0,1fr)_auto_auto]"><div className="relative shrink-0"><Avatar from="#314158" to="#18212f" name={f.persona_name ?? f.steam_id} className="size-14 rounded-full" /><span className="absolute -bottom-0.5 -right-0.5"><PresenceDot online={false} /></span></div><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><p className="truncate font-bold">{f.persona_name ?? f.steam_id}</p></div><p className="mt-0.5 truncate text-xs text-muted-foreground">{f.library_public ? "Library available" : "Library private"}</p><div className="mt-2 flex flex-wrap gap-1"><Chip>{f.games_count} games</Chip><Chip tone="outline">{f.common_games_count} shared</Chip></div></div><div className="hidden text-right sm:block"><p className="font-mono text-[10px] uppercase tracking-widest text-muted-foreground">Compat</p><p className="font-mono text-lg font-black text-primary">{f.taste_match_percent}%</p><p className="mt-0.5 font-mono text-[10px] text-muted-foreground">{f.common_games_count} shared</p></div><div className="flex flex-col gap-2"><button disabled title="Invites are not available from GameFinder." className="rounded-md bg-primary px-3 py-1.5 text-xs font-bold text-primary-foreground">Invite to play</button><button disabled title="Messaging is not available from GameFinder." className="rounded-md border border-border bg-secondary px-3 py-1.5 text-xs font-bold">Message</button></div></div>)}</div></div><div><SectionHeader title="Play together" hint="Shared games reported by Steam friends." /><div className="grid grid-cols-2 gap-4 md:grid-cols-3">{sharedGames.map(({ friend, game }) => <div key={`${friend.steam_id}-${game.appid}`} className="overflow-hidden rounded-xl border border-border bg-surface"><GameCover from="#314158" to="#18212f" title={game.name} className="aspect-video w-full" /><div className="p-3"><div className="flex items-center justify-between gap-2"><p className="truncate text-sm font-bold">{game.name}</p><Chip tone="primary">Shared</Chip></div><button disabled title="Invites are not available from GameFinder." className="mt-3 flex w-full items-center justify-center gap-2 rounded-md bg-secondary py-1.5 text-xs font-bold"><Gamepad2 className="size-3.5" /> Invite</button></div></div>)}</div></div></div><div className="space-y-8 lg:col-span-4">{focus ? <div className="rounded-3xl border border-border bg-surface p-6"><div className="flex items-center gap-4"><Avatar from="#314158" to="#18212f" name={focus.persona_name ?? focus.steam_id} className="size-16 rounded-2xl" /><div><p className="font-bold">{focus.persona_name ?? focus.steam_id}</p><p className="font-mono text-xs text-muted-foreground">Steam friend</p></div></div><div className="my-6 grid grid-cols-3 gap-3 border-y border-border py-4 text-center font-mono"><div><p className="text-[10px] uppercase tracking-widest text-muted-foreground">Compat</p><p className="text-xl font-black text-primary">{focus.taste_match_percent}%</p></div><div><p className="text-[10px] uppercase tracking-widest text-muted-foreground">Shared</p><p className="text-xl font-black">{focus.common_games_count}</p></div><div><p className="text-[10px] uppercase tracking-widest text-muted-foreground">Library</p><p className="text-xl font-black">{focus.games_count}</p></div></div><p className="mb-4 text-sm text-muted-foreground">{focus.library_public ? "Steam reports this library as available." : "Steam reports this library as private."}</p><div className="flex gap-2"><button disabled title="Invites are not available from GameFinder." className="flex-1 rounded-lg bg-primary px-3 py-2 text-sm font-bold text-primary-foreground">Invite to play</button><button disabled title="Messaging is not available from GameFinder." className="grid size-10 place-items-center rounded-lg border border-border"><MessageCircle className="size-4" /></button></div></div> : <div className="rounded-3xl border border-border bg-surface p-6"><p className="text-sm text-muted-foreground">Steam returned no friend records.</p></div>}<div><SectionHeader title="Feed" /><div className="space-y-4 font-mono text-[11px] leading-relaxed"><p className="text-muted-foreground">Steam activity is not available from this API.</p></div></div></div></div></AppShell>;
}
