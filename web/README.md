# GameFinder web

The frontend is a React, Vite, and TanStack Start application. File-based routes
live in `src/routes`; API-backed screens live in `src/features`.

## Development

```bash
npm install
npm run dev
```

Open the local URL printed by Vite. Run checks with:

```bash
npm test
npm run lint
npm run build
```
